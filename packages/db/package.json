{
  "name": "@repo/db",
  "version": "1.0.0",
  "description": "Database utilities and Prisma client for the invertar application",
  "main": "index.ts",
  "scripts": {
    "seed": "ts-node seed.ts"
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "keywords": ["database", "prisma", "postgresql"],
  "author": "rradev",
  "license": "ISC",
  "packageManager": "pnpm@10.13.1",
  "devDependencies": {
    "prisma": "^6.13.0"
  },
  "dependencies": {
    "@prisma/client": "^6.13.0"
  }
}
