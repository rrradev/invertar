{
  "name": "@repo/auth",
  "version": "1.0.0",
  "description": "Authentication utilities for the invertar application",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "keywords": ["auth", "jwt", "password", "bcrypt"],
  "author": "rradev",
  "license": "ISC",
  "packageManager": "pnpm@10.13.1",
  "dependencies": {
    "bcrypt": "^6.0.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0"
  }
}
